package com.assessment.tracker.server.utils.enums;

public enum AssessmentProgress {
    CREATED,
    CHECKED,
    NEEDS_CHANGES,
    TEST_TAKING_PLACE,
    MARKING_STANDARDISED,
    MARKED,
    RESULTS_RETURNED,
    COMPLETE
}
