# API Package

This is where all the Data DTOs and API interfaces will go i.e:

```text
api/
└── user/
    ├── UserService.java
    └── UserDTO.java
```

User Service will be an **Interface** Not a class then we can separate the documentation from the implementation itself